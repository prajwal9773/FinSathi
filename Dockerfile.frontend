FROM node:20-alpine

WORKDIR /app

# Copy package.json and install dependencies
COPY frontend/package*.json ./
RUN npm install

# Copy full source (not build)
COPY frontend ./

# Expose the Vite/React dev server port
EXPOSE 5173

# Run dev mode (keep hot reload etc.)
CMD ["npm", "run", "dev", "--", "--host"]
